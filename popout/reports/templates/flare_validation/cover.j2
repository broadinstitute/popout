{# FLARE validation report cover. The report header (Jinja2 frontmatter)
   is rendered separately; this template provides the title page only. #}
{% include "_base.j2" with context %}

{% set tag = ctx.tag(section.id) %}

# FLARE validation report

### `{{ ctx.bundle.get("run_name", "") }}`

This report compares FLARE against Rye and the upstream RF classifier
on the AoU cohort. FLARE component names are taken **verbatim** from
the panel VCF header, so most sections live in the report's default
label space (`SP5`, MID dropped) with name-based matching and carry
no figure footer. Sections that deviate (for example FLARE vs RF on
`SP6`) print a slim tag under the figure caption; the full
per-section tag is always listed in the provenance appendix. See the
next page for the conventions.

| key | value |
|---|---|
| schema_version | {{ ctx.bundle.get("schema_version", "?") }} |
| generated_at   | {{ ctx.bundle.get("generated_at", "?") }} |
| n_clusters     | {{ ctx.bundle.get("n_clusters", "?") }} |
| n_chroms       | {{ ctx.bundle.get("n_chroms", "?") }} |
| n_artifacts    | {{ ctx.bundle.get("n_artifacts", "?") }} |

*Source: `{{ ctx.bundle_dir }}`*
