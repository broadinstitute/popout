{# (afr, eur, amr) face of the 5-simplex. #}
{% from "_macros.j2" import figure %}
{% set tag = ctx.tag(section.id) %}

## Three-ancestry simplex face

Subset chr1 samples where the chosen 3 ancestries together account for
≥ {{ "%.2f"|format(data.threshold if data.get("present") else 0.95) }}
of the proportion vector. Renormalise to the 3-way face and plot as a
hexbin density. Corner markers and midpoints carry the exact mixture
composition.

{% if chart %}
{{ figure(path=chart, caption="3-way (afr, eur, amr) simplex face, hexbin density.", tag=tag) }}
{% else %}
> **MISSING:** `cohort/cohort_global.tsv`
{% endif %}

{%- if data.get("present") %}
**Kept {{ "{:,}".format(data.n_kept) }} of {{ "{:,}".format(data.n_total) }} chr1 samples on the {{ data["keys"]|join(", ") }} face.**
{%- endif %}
