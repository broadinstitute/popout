"""popout DX (diagnostics) validation suite.

Sibling to ``validation/``. Compares popout against any subset of
{flare, rye, rf}. See ``SCHEMA.md`` for the artifact contract and
``PERFORMANCE_CONTRACT.md`` for the non-negotiable speed rules every
collector in this package must satisfy.
"""
